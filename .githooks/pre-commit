#!/usr/bin/env bash
set -e

echo "==> Type checking..."
uv run pyright src/

echo "==> Tests..."
uv run pytest tests/ -q \
  --ignore=tests/hike/persistence/providers/sqlalchemy \
  --ignore=tests/hike/persistence/providers/pymongo \
  --ignore=tests/hike/persistence/providers/redis

echo "==> Pre-commit checks passed."
